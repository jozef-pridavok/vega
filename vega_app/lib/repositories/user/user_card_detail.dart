// TODO: remove me
/*
import "package:core_flutter/core_dart.dart";

abstract class UserCardRepository {
  Future<UserCard?> read(String userCardId, {bool ignoreCache = false});
  Future<void> create(UserCard userCard);
}
*/
// eof
