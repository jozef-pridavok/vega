// TODO: remove me

/*
import "package:core_flutter/core_dart.dart";
import "package:vega_app/repositories/leaflet/leaflet.dart";

class ApiLeafletRepository extends LeafletRepository {
  final DeviceRepository? deviceRepository;

  ApiLeafletRepository({this.deviceRepository});

  @override
  Future<Leaflet?> read(String clientId, {bool noCache = false}) => throw UnimplementedError();

  @override
  Future<List<Leaflet>?> readAll({Client? client, bool noCache = false}) => throw UnimplementedError();

  @override
  Future<bool> create(Object leaflet) => throw UnimplementedError();
}
*/

// eof
