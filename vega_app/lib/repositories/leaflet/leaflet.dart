// TODO: remove me

/*

import "package:core_flutter/core_dart.dart";

abstract class LeafletRepository {
  Future<List<Leaflet>?> readAll({Client? client, bool noCache = false});
  Future<Leaflet?> read(String clientId, {bool noCache = false});
  Future<void> create(Leaflet leaflet);
}
*/
// eof
